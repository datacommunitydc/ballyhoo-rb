require 'spec_helper'

describe Authorization do
  describe '#find_or_create' do
    context "creating" do
      it "makes a new auth given proper info"
    end

    context "finding" do
      it "returns a proper auth given that one exits"
      it "updates the updated_at when finding"
    end
  end
end
